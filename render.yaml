services:
  - type: worker
    name: crypto-arb-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: CONFIG_PATH
        value: config.json
      - key: PYTHON_VERSION
        value: "3.11"
